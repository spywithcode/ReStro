# TODO: Add Delete Functionality for Tables

## Tasks
- [x] Add `deleteTable` function to `AppContextType` and implement it in `AppProvider` in `src/context/app-context.tsx`
- [x] Update `src/app/admin/tables/page.tsx` to include a "Delete" option in the dropdown menu with an alert dialog for confirmation

## Followup Steps
- [ ] Test the delete functionality to ensure tables are removed from the UI and database
- [ ] Verify error handling for failed deletions
